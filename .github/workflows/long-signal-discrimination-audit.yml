name: ATLAS Long Signal Discrimination Audit

on:
  workflow_dispatch:
  push:
    paths:
      - 'long_signal_discrimination_audit.py'
      - 'tests/test_long_signal_discrimination_audit.py'
      - '.github/workflows/long-signal-discrimination-audit.yml'
      - 'status/**'

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Syntax and regression test
        run: |
          python -m py_compile long_signal_discrimination_audit.py tests/test_long_signal_discrimination_audit.py
          PYTHONPATH=. python tests/test_long_signal_discrimination_audit.py
      - name: Run LONG discrimination audit
        run: python long_signal_discrimination_audit.py
      - name: Persist report
        run: |
          if [ -n "$(git status --porcelain -- status/long-signal-discrimination-audit.json)" ]; then
            git config user.name 'github-actions[bot]'
            git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
            git add status/long-signal-discrimination-audit.json
            git commit -m 'data: refresh long signal discrimination audit'
            git push
          else
            echo 'No report change to persist.'
          fi
